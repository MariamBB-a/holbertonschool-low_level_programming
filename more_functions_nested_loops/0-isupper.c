#include "main.h"
#include <stdio.h>

/**
 * Write a function that checks for uppercase character.
 * @c: The character to print
 * Return: 0 otherwise
 */
int _isdigit(int c)
{
    if (c >= 'A' && c <= 'Z')

    return (1);
    else
    return (0);
}
