#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: pointer to the string
 *
 * Return: length of the string
 *
 */

void _puts(char *str)
{
		printf( "%s\n", str);
}
