#include <stdio.h>


int main(void)
{
        char = 'a'
        while <= 'z'
        {
                if (char ! = 'q' && char ! = 'e')
                        putchar (char);
                char++;
        }
        putchar ('\n')
        return ('0');
}
