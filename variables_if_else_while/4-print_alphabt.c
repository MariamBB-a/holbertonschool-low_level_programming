#include <stdio.h>


int main(void)
{
        char ch = 'a';
        while (ch <= 'z')
        {
                if (char ! = 'q' && char ! = 'e')
                        putchar (ch);
                ch++;
        }
        putchar ('\n');
        return '0';
}
