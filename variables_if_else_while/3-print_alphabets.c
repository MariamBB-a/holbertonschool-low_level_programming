#include <stdio.h>
#include <stdlib.h>

/*
 * main - print letters
 * return:to 0
 */

int main(void)
{
    char ch;

for( ch = 'a'; ch <= 'z'; c++ )
putchar(ch);

for( ch = 'A'; ch <= 'Z'; c++ )
putchar(ch);

putchar('\n');

return (0);
}
